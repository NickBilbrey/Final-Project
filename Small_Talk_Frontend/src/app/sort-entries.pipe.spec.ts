import { SortEntriesPipe } from './sort-entries.pipe';

describe('SortEntriesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortEntriesPipe();
    expect(pipe).toBeTruthy();
  });
});

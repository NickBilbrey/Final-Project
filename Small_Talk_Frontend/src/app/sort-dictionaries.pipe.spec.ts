import { SortDictionariesPipe } from './sort-dictionaries.pipe';

describe('SortDictionariesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortDictionariesPipe();
    expect(pipe).toBeTruthy();
  });
});
